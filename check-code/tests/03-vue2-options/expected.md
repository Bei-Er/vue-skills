# 期望发现的问题

## 错误

| # | 问题 | 分类 | 位置 |
|---|------|------|------|
| 1 | `data` 是对象而非函数，多组件实例会共享同一份数据 | 响应性 | 第 3 行 |
| 3 | watch 使用箭头函数，`this` 指向外部作用域而非组件实例 | 响应性 | 第 22 行 |
| 4 | `val[0] = {...}` 数组索引直接赋值，Vue 2 无法检测变更 | 响应性 | 第 28 行 |
| 5 | `this.newProp = val.length` 动态添加属性，Vue 2 无法使其响应 | 响应性 | 第 31 行 |
| 6 | `this.items.push(...)` 直接修改 props | 响应性 | 第 38 行 |
| 8 | `delete this.count` 删除响应式属性，应使用 `this.$delete` | 响应性 | 第 43 行 |

## 性能

| # | 问题 | 分类 | 位置 |
|---|------|------|------|
| 9 | `v-for` 使用 `index` 作为 key，列表增删时会导致状态错乱 | 响应性 | 第 50 行 |

## 建议

| # | 问题 | 分类 | 位置 |
|---|------|------|------|
| 2 | computed 中包含 `console.log` 副作用 | 计算与监听 | 第 17 行 |
| 7 | `this.list.length` 可替换为 `computed` | 计算与监听 | 第 39 行 |
